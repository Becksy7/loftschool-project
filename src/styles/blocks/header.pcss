.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  &__menu {
    @include tablets {
      display: none;
    }
  }
  &__buttons {
    padding-top: 16px;
    @include phones {
      display: none;
    }
  }
}
